import { Route, Routes } from "react-router-dom"

export const UserRoutes = () => {

    return(<>
            sick homepage
    <Routes>
        <Route>
        </Route>
    </Routes>
    </>)
}